<UserControl
    x:Class="ChinChessClient.UserControls.MovePahtCtrl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:chinesechess="clr-namespace:ChinChessCore.Commands;assembly=ChinChessCore">
    <Border
        Margin="5"
        Background="#88CCCCCC"
        CornerRadius="10">
        <ListBox
            VerticalAlignment="Bottom"
            HorizontalContentAlignment="Left"
            FontSize="26"
            ItemsSource="{Binding CommandStack}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            VirtualizingPanel.IsVirtualizing="True">
            <ListBox.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="Black" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRed}" Value="True">
                            <Setter Property="Foreground" Value="Red" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type chinesechess:MoveCommand}">
                    <TextBlock Style="{StaticResource {x:Type TextBlock}}">
                        <TextBlock Text="{Binding Index}" />
                        ]
                        <TextBlock Text="{Binding From}" />
                        =>
                        <TextBlock Text="{Binding To}" />

                        <TextBlock Text="{Binding Notation}" />
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Border>
</UserControl>
